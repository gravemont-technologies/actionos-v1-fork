{
  "version": 3,
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x",
      "maxDuration": 10
    }
  {
    "version": 2,
    "builds": [
      { "src": "api/**/*.ts", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1.ts" }
    ]
  }
